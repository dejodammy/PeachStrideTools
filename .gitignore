# Dependencies
node_modules/

# Build output
client/dist/

# Runtime campaign data (recipient lists, generated PDFs, send logs — not source)
server/data/campaigns/*
!server/data/campaigns/.gitkeep

# Logs
*.log
npm-debug.log*

# Local env files (never commit SMTP credentials)
.env
.env.local

# Personal test/demo spreadsheet (contains a real email address)
demo_recipients.xlsx

# Real employee/candidate data that ended up in the repo root and in an old
# cvExtract/ snapshot by accident — purged from history; never again.
/letters/
/employee.xlsx
/employees.xlsx
/empp.xlsx
/ses_test_recipients.xlsx
/guarantor_form.pdf
/handbook.pdf
/cvExtract/

# OS/editor cruft
.DS_Store
Thumbs.db

# Runtime CV extraction jobs (uploaded CVs + generated spreadsheets)
server/data/extractions/*
!server/data/extractions/.gitkeep

# Python
__pycache__/
*.pyc
